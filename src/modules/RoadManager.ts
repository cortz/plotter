import type { TileData } from '../types'

/** Roads have been removed. This is a no-op kept for any legacy imports. */
export function recomputeRoads(grid: TileData[][]): TileData[][] {
  return grid
}
